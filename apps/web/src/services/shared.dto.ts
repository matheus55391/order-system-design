export interface TenantRefDto {
  id: string;
  name: string;
  slug: string;
}

export interface MessageResponseDto {
  message: string;
}

export interface SuccessResponseDto {
  success: boolean;
}
